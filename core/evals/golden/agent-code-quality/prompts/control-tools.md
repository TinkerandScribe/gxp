# Control arm — tool-using implementer (no GXP)

You are fixing a **multi-file** coding task. You **may and should use tools**
(read files, edit, run shell commands).

## Instructions

1. Your editable project root is the trial workspace you were given (copy of
   `starter/`).  
2. Read `prompt.md` in the task definition (or the pasted prompt).  
3. Implement until done. Use tools freely.  
4. **Do not** follow GXP / write a formal brief unless you want to.  
5. **Do not** look for or invent `hidden_tests/`.  
6. Prefer running the workspace public verify command if documented.

## Done

Say when you believe the package meets the prompt spec.
