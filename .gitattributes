# Normalize all text to LF in the repo; Git converts on checkout.
# Keeps clones byte-identical across OSes and kills CRLF/LF diff churn.
* text=auto eol=lf

# Treat common binaries as binary (never normalized).
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.plugin binary
